{
    "tasks": [
       {
           "label": "Build Philo",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/philo"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Build Philo_Bonus",
             "type": "shell",
             "command": "make",
             "options": {
                 "cwd": "${workspaceFolder}/philo_bonus"
             },
             "problemMatcher": [
                 "$gcc"
             ],
             "group": {
                 "kind": "build",
                 "isDefault": true
             }
         }
    ],
    "version": "2.0.0"
}